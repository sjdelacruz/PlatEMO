function especializedTables = classifyResults(results,nExperiments, nAlgorithms,nIndicators,nMetrics,runs)

nRows = nExperiments * nAlgorithms;
values = zeros(nExperiments, nAlgorithms);
 
[~,rowSize] = size(results(1,:));
for row=1 : nRows
    rowi = result(row,:);
    
    for ind=4 : 
        
        
    end
 
end
end

